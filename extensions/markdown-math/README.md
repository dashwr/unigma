# Markdown Math

**Notice:** This extension is bundled with unigma. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to unigma's built-in markdown preview and markdown cells in notebooks.
